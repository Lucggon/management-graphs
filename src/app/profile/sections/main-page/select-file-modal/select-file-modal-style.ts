import { MatDialogConfig } from "@angular/material/dialog";
import { SelectFileModalComponent } from "./select-file-modal.component";

export const modalStyle: MatDialogConfig<SelectFileModalComponent> = {
  minWidth: 400,
  minHeight: 500,
}
